# Dynamic Cart Valuation Engine (Elixir + Rust)

Высоконагруженный движок динамического расчета стоимости корзины в реальном времени, спроектированный с использованием гибридной архитектуры **Elixir (OTP/Distributed Orchestration)** и **Rust (NIF/High-Performance Computation)**.

Проект разработан как технологическое решение архитектурной проблемы деградации производительности при расчете сложных, пересекающихся правил лояльности и скидок в условиях экстремальной конкурентной нагрузки (ритейл реального времени / стек ecom.tech).

---

## 1. Актуальность и бизнес-контекст

В e-commerce платформах (таких как Самокат, Мегамаркет или Купер) расчет стоимости корзины — одна из самых критичных и вычислительно сложных операций. При изменении состава корзины система должна в пределах единиц миллисекунд:

1. Проверить десятки пересекающихся промокодов и правил лояльности.
2. Исключить несовместимые акции и рассчитать оптимальную скидку для сотен позиций.
3. Проверить налоговые и региональные ограничения.

### Системная проблема:

- **I/O-bound vs CPU-bound:** Виртуальная машина BEAM идеально оптимизирована для управления миллионами веб-сокетов и параллельных сессий пользователей (I/O-bound). Однако комбинаторные расчеты правил лояльности на чистом Elixir создают высокую нагрузку на CPU (CPU-bound).
- **Деградация задержек (Latency Spikes):** Выполнение тяжелой математики на Elixir-процессах приводит к перегрузке планировщиков (schedulers), росту задержек на хвосте распределения времени отклика (tail latency) и увеличению частоты сборки мусора (GC) из-за копирования термов.

---

## 2. Архитектура и три сценария оптимизации

Проект реализует паттерн **"Endurance Stack"** и решает три фундаментальных архитектурных вызова:

### Сценарий I: Оптимизация маршалинга памяти (Zero-Copy & Lazy List Iteration)

Для базового расчета стоимости корзины стандартное декодирование карт Elixir (`%{item_id: ..., price: ...}`) требует выполнения дорогостоящих хеш-поисков Erlang VM для каждого ключа.

- **Решение:**
  - Обмен данными переведен на плоские кортежи **`NifTuple`**, транслируемые напрямую в непрерывные массивы памяти C на нативном уровне.
  - Обход списка корзин на стороне Rust выполняется лениво через **`ListIterator`**, что предотвращает аллокацию промежуточных нативных векторов `Vec`.
  - Поля строк-ключей `item_id` декодируются по принципу Zero-Copy с типом **`&'a str`**, ссылаясь напрямую на бинарные данные в куче BEAM без повторного выделения памяти под новые owned-строки `String`.

### Сценарий II: Оптимальное комбинаторное дерево поиска (NP-сложная задача / Branch and Bound)

Поиск оптимального сочетания скидок при наличии **конфликтующих (взаимоисключающих) промокодов** представляет собой NP-сложную задачу (аналог задачи о рюкзаке).

- **Решение:**
  - На стороне Rust реализован рекурсивный обход дерева состояний на базе алгоритма **Backtracking (поиск в глубину)**.
  - Проверка конфликтов правил заменена с линейного сканирования векторов на **побитовые маски (`u64`)**. Проверка конфликта нового правила со всеми ранее выбранными выполняется процессором за **1 такт** через инструкцию побитового `AND` (`&`).
  - Внедрен алгоритм **ветвей и границ (Branch and Bound)**: на каждом шаге рекурсии рассчитывается сумма потенциально возможных оставшихся скидок; если текущая сумма дисконта плюс остаток не способны побить уже найденный рекорд, ветвь рекурсии мгновенно отсекается.
  - Все операции выполняются исключительно на стеке без выделения динамической памяти на куче Rust (Zero Allocations).

### Сценарий III: Обход бутылочного горлышка процессов (Lock-Free Shared Registry)

При удержании состояния корзины внутри стандартного `GenServer` параллельные запросы на чтение и расчет стоимости выстраиваются в очередь сообщений (mailbox) процесса, обрабатываясь строго последовательно.

- **Решение:**
  - Применен паттерн **CQRS**: команды на мутацию корзины (запись) идут через `GenServer` (`CartServer`), а конкурентные запросы на чтение и расчет стоимости идут напрямую в обход очереди сообщений.
  - На стороне Rust создан разделяемый ресурс `SharedCartRegistry` на базе потокобезопасной сегментированной хеш-карты **`DashMap`**. Elixir-процессы читают состояние корзин напрямую из DashMap в обход очередей сообщений Erlang-процессов.

---

## 3. Распределенный кластер и локальность данных (Data Locality)

Для работы в масштабах распределенной сети (Kubernetes) проект расширен до отказоустойчивого кластера с поддержкой автоматического объединения нод и динамической маршрутизации запросов.

```text
               [Запрос на расчет корзины "cart_1" пришел на node3]
                                      │
                                      ▼
                        ┌───────────────────────────┐
                        │      Horde.Registry       │ (Где живет "cart_1"?)
                        └─────────────┬─────────────┘
                                      │
                                      ▼
                     (Корзина "cart_1" живет на node1)
                                      │
                                      ▼
                        ┌───────────────────────────┐
                        │         :rpc.call         │ (Вызов расчета на node1)
                        └─────────────┬─────────────┘
                                      │
                                      ▼
                       ┌─────────────────────────────┐
                       │  Rustler NIF на node1       │ (Чтение из локального DashMap)
                       └─────────────┬─────────────┘
                                      │
                                      ▼
                             (Результат f64)
                                      │
                                      ▼
                         [Возврат ответа на node3]
```

### Ключевые компоненты кластера:

1. **`libcluster` (Node Discovery):** Обеспечивает автоматическое сетевое обнаружение контейнеров в приватной мостовой сети Docker и их объединение в единую распределенную Erlang-сеть.
2. **`Horde` (Distributed Registry & Supervisor):** Распределенный реестр и динамический супервизор процессов на базе CRDT. `Horde` гарантирует уникальность процесса `CartServer` в рамках всего кластера и равномерно распределяет процессы по выжившим нодам.
3. **Распределенный CQRS-маршрутизатор (`DistributedRouter`):**
   - **Запись:** Прозрачно направляет изменения на ноду-владелец конкретного `CartServer`.
   - **Чтение (RPC-оптимизация):** Если процесс корзины находится на удаленной ноде, роутер делает сетевой RPC-вызов `:rpc.call` на целевую ноду. При этом сырой список правил `raw_rules` безопасно сериализуется, передается по сети и **компилируется на месте на целевой ноде**. Это исключает пересылку тяжелых C-указателей NIF-ресурсов через сеть (что привело бы к ошибке `badarg`) и предотвращает сетевой перенос сырых данных корзин.

---

## 4. Системный анализ и качество кода (QA Pipeline)

Проект интегрирует строгий промышленный стандарт статического анализа и безопасности, принятый в ecom.tech.

### Конвейер статических проверок Elixir:

- **Credo (Строгий режим):** Проверка архитектурных антипаттернов, стиля и структуры модулей.
- **Dialyxir (Dialyzer):** Строгий статический анализ типов на основе спецификаций `@spec`.

### Гарантии безопасности Rust (Clippy & Compiler):

Паника (`panic!`) в неуправляемом коде NIF приводит к мгновенному аварийному завершению всей Erlang VM. Для обеспечения стабильности на уровне компиляции настроены жесткие ограничения:

- **Запрет паникующих макросов:** `unwrap_used = "deny"`, `expect_used = "deny"`, `panic = "deny"`. Любая обработка ошибок возвращает `Result<T, E>` в Elixir.
- **Запрет явного разыменования NIF-ресурсов:** `clippy::explicit-auto-deref` — гарантирует использование встроенного механизма безопасного автоматического разыменования умных указателей `ResourceArc`.
- **Защита от сбоя тредов:** `indexing_slicing = "deny"` — предотвращает выход за границы векторов и слайсов (out-of-bounds).
- **Прохождение panic-границы:** Для структуры `SharedCartRegistry` вручную имплементированы маркерные типажи `RefUnwindSafe` и `UnwindSafe` для безопасной передачи ресурса через границу `catch_unwind` в Rustler.

---

## 5. Структура проекта (Cargo Workspace)

Проект реализован в виде полиглотного монорепозитория:

```text
├── mix.exs                     # Конфигурация Elixir-проекта и зависимостей
├── .credo.exs                  # Строгая конфигурация Credo
├── config/
│   └── config.exs                  # Конфигурация Elixir-приложения
├── lib/                        # Elixir-компонент
│   ├── cart_engine/
│   │   ├── application.ex      # Дерево супервизии (Supervisor, Horde Registry)
│   │   ├── cart_server.ex      # Процесс-мутатор GenServer (Write-path)
│   │   ├── cluster_connector.ex# Коннектор нод Horde
│   │   ├── distributed_router.ex# Распределенный CQRS-маршрутизатор
│   │   ├── elixir_combinatorial.ex # Чистая комбинаторная логика на Elixir
│   │   ├── valuation.ex        # Логика базового Elixir (для бенчмарков)
│   │   └── rust_bridge.ex      # Загрузка NIF и сопоставление типов
│   └── cart_engine.ex          # Публичный API движка расчета
└── native/                     # Rust-компонент (Cargo Workspace)
    ├── Cargo.toml              # Конфигурация воркспейса (Clippy правила)
    ├── clippy.toml             # Глобальные настройки Clippy (MSRV 1.80.0, лимиты сложности)
    └── cart_engine_nif/        # Крит NIF-адаптера
        ├── Cargo.toml
        └── src/
            └── lib.rs          # Логика сопряжения с BEAM, DashMap и PromotionTrie
```

---

## 6. Установка и запуск

### Требования к окружению:

- **macOS** (Apple Silicon) или **Linux**
- **Erlang/OTP 28+** & **Elixir 1.19.5+**
- **Rust 1.80+** (компилятор `rustc`, менеджер `cargo`)
- **OrbStack** или **Docker** (для запуска кластера)

### 1. Локальная сборка проекта и тестирование QA-пайплайна

```bash
# Установка Elixir-зависимостей
mix deps.get

# Проверка качества кода (CI Simulation)
make ci

# Запуск тестов (включая сетевые интеграционные тесты)
mix test
```

### 2. Запуск распределенного кластера из 3 нод в Docker (через OrbStack)

```bash
# Сборка и старт кластера
docker compose up --build
```

_Контейнеры `cart_node_1`, `cart_node_2` и `cart_node_3` запустятся, сформируют Erlang-сеть и автоматически синхронизируют реестры Horde._

### 3. Интерактивное подключение к ноде кластера (Remote Console)

Вы можете войти в интерактивную консоль работающего контейнера прямо с хост-машины:

```bash
docker exec -it cart_node_1 bin/cart_engine remote
```

---

## 7. Результаты бенчмаркинга и системный анализ

Замеры проводились на оборудовании **Apple M5 (10 ядер, 16 ГБ RAM)** под управлением **Elixir 1.19.5, Erlang 29.0.1 (JIT Enabled)** с использованием библиотеки **Benchee**.

### БЕНЧМАРК I: Сценарий базового расчета стоимости (Остатки на DashMap)

#### Тест 1: Минимальная база правил лояльности (Best-Case для Elixir)

Активные правила находятся в начале списка. Работает эффект раннего выхода (_early exit_).

```text
##### With input Large Cart (150 items) #####
Name                            ips        average  deviation         median         99th %
elixir_pure_valuation      224.36 K        4.46 μs    ±99.94%        4.25 μs        6.92 μs
rust_nif_valuation         150.84 K        6.63 μs   ±261.15%        6.46 μs        8.04 μs

Comparison:
elixir_pure_valuation      224.36 K
rust_nif_valuation         150.84 K - 1.49x slower +2.17 μs

Memory usage statistics:
Name                     Memory usage
elixir_pure_valuation         5.88 KB
rust_nif_valuation          0.0156 KB - 0.00x memory usage -5.86719 KB
```

#### Тест 2: Реалистичная база правил (Worst-Case для Elixir, 100 правил)

Активные правила находятся в самом конце списка. Elixir выполняет линейный поиск $O(N \times M)$.

```text
##### With input Large Cart (150 items) #####
Name                            ips        average  deviation         median         99th %
rust_nif_valuation         191.00 K        5.24 μs    ±23.90%        5.13 μs        7.08 μs
elixir_pure_valuation        9.19 K      108.86 μs     ±6.47%      107.88 μs      134.56 μs

Comparison: 
rust_nif_valuation         191.00 K
elixir_pure_valuation        9.19 K - 20.79x slower +103.63 μs

Memory usage statistics:

Name                     Memory usage
rust_nif_valuation          0.0156 KB
elixir_pure_valuation         5.88 KB - 376.50x memory usage +5.87 KB
```

#### Разбор результатов бенчмарка I:

- В Тесте 1 на малых объемах Elixir работает быстрее из-за накладных расходов на вызов NIF (Context Switch в ОС занимает `~1.6 мкс`).
- В Тесте 2 при росте базы правил до 100 Elixir начинает тратить время на линейный перебор списка в памяти, замедляясь до `108.86 μs`. В это же время Rust `HashMap` сохраняет стабильную константную сложность $O(1)$, выполняя расчет за прежние **`~5.24 μs`** (производительность выше в **20.79 раз**).

---

### БЕНЧМАРК II: Сценарий комбинаторного Backtracking (NP-сложная задача)

Рекурсивный поиск в глубину (Backtracking) по дереву из 15 применимых правил с перекрестными конфликтами ($2^{15}$ состояний).

```text
Name                                ips        average  deviation         median         99th %
rust_nif_combinatorial         238.52 K        4.19 μs    ±56.04%        4.04 μs        8.25 μs
elixir_pure_combinatorial        3.32 K      300.76 μs     ±6.89%      299.79 μs      371.84 μs

Comparison: 
rust_nif_combinatorial         238.52 K
elixir_pure_combinatorial        3.32 K - 71.74x slower +296.57 μs

Memory usage statistics:

Name                         Memory usage
rust_nif_combinatorial          0.0156 KB
elixir_pure_combinatorial       316.62 KB - 20263.50x memory usage +316.60 KB
```

#### Разбор результатов бенчмарка II:

На тяжелых вычислительных задачах (рекурсивный обход дерева с плавающей запятой) Rust опережает Elixir **в 71.74 раза** по чистой скорости. При этом чистая Elixir-реализация на каждый вызов вынуждена выделять **316.62 КБ** временной памяти под рекурсивные списки на куче BEAM. Rust NIF, за счет побитовых масок конфликтов и алгоритма отсечения Branch and Bound, расходует ровно **16 байт**.

---

### БЕНЧМАРК III: Обход бутылочного горлышка (Параллельная нагрузка в 10 процессов)

Имитация конкурентного шторма на независимые корзины под параллельной нагрузкой в 10 Erlang-процессов. Сравнивается чтение через последовательный mailbox GenServer и параллельный lock-free доступ к DashMap напрямую.

```text
Name                                 ips        average  deviation         median         99th %
dashmap_concurrent_reads        234.39 K        4.27 μs   ±737.51%        2.58 μs       12.21 μs
genserver_serialized_reads       49.18 K       20.33 μs    ±60.46%       17.75 μs       57.96 μs

Comparison: 
dashmap_concurrent_reads        234.39 K
genserver_serialized_reads       49.18 K - 4.77x slower +16.07 μs

Memory usage statistics:

Name                               average  deviation         median         99th %
dashmap_concurrent_reads           0.41 KB     ±0.00%        0.41 KB        0.41 KB
genserver_serialized_reads         1.07 KB     ±2.08%        1.07 KB        1.09 KB

Comparison: 
dashmap_concurrent_reads           0.41 KB
genserver_serialized_reads         1.07 KB - 2.64x memory usage +0.66 KB
```

#### Разбор результатов бенчмарка III:

Устранение флага `schedule = "DirtyCpu"` (который добавлял `~15 мкс` на переключение потоков ОС для сверхбыстрых задач) и распределение запросов по сегментам `DashMap` (исключившее Cache Line Bouncing процессора) позволило нативному конкурентному чтению показать прирост производительности **в 4.77 раза (на 377%) по сравнению с GenServer**. Медианное время расчета составило рекордные **`2.58 μs`**.

#### Системный вывод по предотвращению GC-давления (Latency p99):

При пиковой нагрузке в **100 000 RPS** (запросов в секунду):

- **В Elixir:** Система генерирует **~588 МБ временной памяти в секунду** на куче Erlang-процессов. Это приводит к непрерывному запуску сборщика мусора (GC) во всех потоках шедулеров, вызывая просадки по латентности ( latency spikes) на 99.9-м процентиле.
- **В Rust (NIF):** Вычисления генерируют **0 байт мусора** на Erlang-куче. График времени отклика остается стабильным и плоским, гарантируя предсказуемый tail latency в рамках SLA.
